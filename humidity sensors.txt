/*
    analogWrite (ledB,0);
  }
  
  if(humidity>20 && humidity<=30){
    analogWrite (ledR,0);
    analogWrite (ledG,100);
    analogWrite (ledB,0);
  }
  
  if(humidity>30 && humidity<=40){
    analogWrite (ledR,0);
    analogWrite (ledG,0);
    analogWrite (ledB,100);
  }
}